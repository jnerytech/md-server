# Rule: Use AskUserQuestion for clarifications

Whenever you need to ask the user something — to clarify requirements,
confirm decisions, or collect information before acting — use the
`AskUserQuestion` tool instead of asking in the chat.
